# This config file contains exec-once statements for autorunning applications

exec-once = waybar
exec-once = hyprpaper
exec-once = dunst
exec-once = systemctl --user start plasma-polkit-agent.service
